const initialState = {
    isLoading: false,
    projects: [],
    allProjects: [],
    project: [],
  };
  
  export const RegReducer = (state = initialState, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };