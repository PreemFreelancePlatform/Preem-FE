import React from 'react'

export const Office = () => {

    // when jobs are accepted and active this is where they go
    // free uploads progress of job, info etc.
    return (
        <div>
            
        </div>
    )
}
